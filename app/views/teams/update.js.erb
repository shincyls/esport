$('#<%=@team.model_name.name%>-table').dataTable().fnDestroy();
$("#modal-form").modal('hide');
$('#flash-message').html("<%= j (render 'statics/message') %>");
$('#team-logo-<%=@team.id%>').html("<%= j (image_tag(@team.logo, width: 48) if @team.logo.attached?) %> ");